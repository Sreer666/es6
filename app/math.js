let add=(a,b)=>{
  return a+b;}
let mutiply=(a,b)=>{
  return a*b;}
export {add, mutiply};// if you export 2 function methods go for it.
export default add;// if you want to export only one function method go with default
